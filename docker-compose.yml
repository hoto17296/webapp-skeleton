version: '3'
services:

  db:
    image: postgres:alpine

  redis:
    image: redis:alpine

  web:
    build: .
    ports:
      - 8080:80
    volumes:
      - .:/app
    environment:
      DEBUG: express:*
      SECRET_KEY: deadbeef
      DATABASE_URL: postgresql://postgres@db/postgres
      REDIS_URL: redis://redis/
    command: npm run dev
    depends_on:
      - db
      - redis
